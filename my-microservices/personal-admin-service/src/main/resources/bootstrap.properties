# Spring Cloud Config Client settings
spring.application.name=personal-admin-service
spring.cloud.config.uri=http://localhost:8888 # Address of the Config Server

# Optional: If your Config Server uses profiles
# spring.profiles.active=dev

# Optional: If your Config Server requires authentication
# spring.cloud.config.username=your-config-server-username
# spring.cloud.config.password=your-config-server-password

# Optional: Retry settings if Config Server is not immediately available on startup
spring.cloud.config.fail-fast=true
# spring.cloud.config.retry.initial-interval=1000
# spring.cloud.config.retry.max-attempts=6
# spring.cloud.config.retry.multiplier=1.1
# spring.cloud.config.retry.max-interval=2000
