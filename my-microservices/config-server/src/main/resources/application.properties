server.port=8888

spring.cloud.config.server.git.uri=file://${user.home}/config-repo
# In a real setup, you might use something like:
# spring.cloud.config.server.git.uri=https://github.com/your-username/config-repo.git
# spring.cloud.config.server.git.username=your-username
# spring.cloud.config.server.git.password=your-password

# Optional: For local testing with a file system backend, you can create a 'config-repo' directory
# at the root of the 'my-microservices' project and place your .properties or .yml files there.
# For example, a file named 'my-service.properties' in 'config-repo' would be accessible
# via http://localhost:8888/my-service/default

logging.level.org.springframework.cloud.config=DEBUG
