# Disable Eureka client for API Gateway tests to allow context loading without a running Eureka server
eureka.client.enabled=false
spring.cloud.discovery.enabled=false
spring.cloud.gateway.discovery.locator.enabled=false
spring.cloud.gateway.discovery.locator.url-expression='http://localhost:1234' # dummy url, won't be used.
